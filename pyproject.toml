[project]
name = "school-complaint-llm-negative-transfer"
version = "1.0.0"
description = "Reproducibility analyses for school-complaint LLM domain adaptation and retrieval experiments"
requires-python = ">=3.11"
dependencies = [
  "numpy==2.3.3",
  "pandas==2.3.3",
  "scipy==1.16.2",
]

[tool.setuptools]
packages = ["analysis"]
